# Copia este archivo a .env y exporta las variables antes de correr generate_avatar.py
# source .env  (o usa `export $(cat .env | xargs)` / python-dotenv)

ELEVENLABS_API_KEY=
REPLICATE_API_TOKEN=
FAL_KEY=
HEDRA_API_KEY=
